apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: counter-lab
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis
