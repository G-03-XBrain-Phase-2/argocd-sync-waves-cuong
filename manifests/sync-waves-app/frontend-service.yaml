apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: database-frontend-lab
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  ports:
  - port: 80
  selector:
    app: frontend
