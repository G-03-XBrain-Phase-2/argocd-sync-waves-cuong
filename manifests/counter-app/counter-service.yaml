apiVersion: v1
kind: Service
metadata:
  name: counter-service
  namespace: counter-lab
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: counter-web
